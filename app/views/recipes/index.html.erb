<div>
  <%= link_to 'Create new recipe', "/recipes/new", method: :get %>
</div>

</p>

<% @recipes.each do |recipe| %>
  <% if @notice %>
    <h2><%= @notice %></h2>
  <% end %>

  <li><%= recipe.title %>
    <%= link_to 'Show', recipe, method: :get, class: 'show' %>
  </li> 
<% end %>

